<UserControl x:Class="Personas.NuevaPersona"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Personas"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel LastChildFill="False">
        <StackPanel DockPanel.Dock="Top" Margin="10,0,0,0">
            <TextBlock>Nombre</TextBlock>
            <TextBox></TextBox>
            <TextBlock>Edad</TextBlock>
            <TextBox></TextBox>
            <TextBlock>Nacionalidad</TextBlock>
            <StackPanel Orientation="Horizontal">
                <ComboBox Width="150"  ItemsSource="{Binding Path=ListaNacionalidades}"></ComboBox>
                <Button Command="{Binding Path=AbrirAñadirNacionalidadCommand}" Margin="5,0" Padding="5,0">+</Button>
            </StackPanel>
            <Button Height="30" Width="100">Aceptar</Button>
        </StackPanel>
    </DockPanel>
</UserControl>
